{
  "name": "@hamcker/styled-tailwind",
  "version": "1.0.3",
  "description": "Tiny styled() for Tailwind + React (works with intrinsic elements and custom components)",
  "author": {
    "name": "Hamed Zakeri Miyab",
    "email": "hamcker@gmail.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hamcker/styled-tailwind.git"
  },
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.cjs"
    }
  },
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun run build:js && bun run build:types",
    "build:js": "bun run build:esm && bun run build:cjs",
    "build:esm": "esbuild ./src/index.ts --outfile=./dist/index.js --format=esm --sourcemap --bundle --external:react --external:react/jsx-runtime --external:react/jsx-dev-runtime",
    "build:cjs": "esbuild ./src/index.ts --outfile=./dist/index.cjs --format=cjs --sourcemap --bundle --external:react --external:react/jsx-runtime --external:react/jsx-dev-runtime",
    "build:types": "tsc -p tsconfig.build.json",
    "publish": "bun publish",
    "clean": "rm -rf dist",
    "test": "bun test"
  },
  "peerDependencies": {
    "react": ">=18",
    "tailwindcss": ">=3.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": ">=18",
    "esbuild": "^0.25.9",
    "react": ">=18",
    "react-dom": ">=18",
    "typescript": "^5.0.0"
  }
}
